import React from 'react';
import Header from "../../components/Header/Header";

const PricePage = ({ brandName, navList }) => (
  <Header brandName={brandName} topicList={navList} />
);

export default PricePage;